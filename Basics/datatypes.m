clc, clearvars, close all;

%double
x = -1.5;

%integer
y = int32(x);

%unsigned integer
z = uint32(x);

%string
s = "hello world";

%char
c = 's';

%booleans
t = true;
f = not(t); %we are negating true therefore we get false we can also only write false


%to see all the variable we have and their info
whos
