%we declare a function that we will use later in another file we must be
%careful because the name of this file must be the same as the name of the
%function we are defining
%we set it equal to something if we want an equivalent of a return 
function result = function1(x, y)
    %algorithm definition
    result = x * y;
end